// import Head from "next/head";
import { Box } from "@material-ui/core";
import Layout from "components/Layout";

const Home = () => {
  return (
    <Layout>
      <Box px={1}>
        <h1>Home Page</h1>
      </Box>
    </Layout>
  );
};

export default Home;
